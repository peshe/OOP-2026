#pragma once

template <typename Type>
Type min(Type a, Type b);

// Дефиниции (имплементации) на шаблонни функци трябва да стоят изцяло в хедър файл!
// В обия случай не искаме да пишем дефиниции (имплементации) на шаблонни функции в cpp файл!

// Това е един хак с който можем все пак да разделим декларацията от дефиницията
// Важно е имплементационния файл да НЕ е с разширение .cpp, .c, .cxx, c++ !!!
// Може да използвате например: ipp, impl, inc, inl, i++, и т.н.
#include "Utility.ipp"
