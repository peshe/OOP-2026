#pragma once

#include <iostream>

enum police_rank 
{
    //...
};

enum threat_level
{
    //...
};

// this is bolier plate. You can build the app entirly different
namespace department 
{
    // run the program
    void run();

    // init the data
    void init();
};