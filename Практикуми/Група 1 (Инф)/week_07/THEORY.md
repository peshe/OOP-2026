> **Disclaimer:** Тази седмица информацията е много обширна и файлът стана огромен. Не се плашете, тук поставям съдържанието на файла, прочетете си го с ваше темпо. Това е една от най-обширините и интерсни части на курса ни този семестър!

**Съдържание:**  
- [Жизнен цикъл на обекти](#1)
    - [Преговор](#1.1)
    - [Инициализиращ списък](#1.2)
    - [Видове конструкотри](#1.3)
        - [Конструктор по подразбиране](#1.3.1)
        - [Копиращ конструктор](#1.3.2)
        - [Move конструктор](#1.3.3)
        - [Експлицитни конструктори](#1.3.4)
    - [Оператори за присвояване](#1.4)
        - [Копиращ оператор за присвояване](#1.4.1)
        - [Move оператор за присвояване](#1.4.2)
    - [The Rule of X](#1.5)
- [Изключения](#2)
    - [Гаранции за сигурност](#2.1)
        - [Noexcept](#2.1.1)
        - [Strong Exception Safety](#2.1.2)
        - [Weak Exception Safety](#2.1.3)
- [Ресурси](#3)


<a id="1"></a>
## Жизнен цикъл на обекти

В предното упражнение се запознахме с това какво представлява повърхностно ООП и каква е идята му. Разгледахме какво са класовете и как се различват от досега познатите ни структурите и видяхме как можем да дефинираме изцяло нови типове чрез тях. Най-важната концепция, с която се запознахме, обаче, беше създаването и зачистването на обекти чрез конструктори и деструктори.

<a id="1.1"></a>
**Как работехме с Конструктори?**

Когато искахме да създадем обект от някакъв тип структура можеше да използваме инициализиращ списък, тъй като всичките елементи по подразбиране в една структура са видими за външния свят и съответно следния синтаксис е много удобен и лесен за използване:

~~~.cpp
struct Circle {
    int radius;
    int center_x;
    int denter_y;
};

Circle tiny_circle = {2,0,0};
~~~

Данните в инициализиращия списък съответстват 1:1 на тези в структурата и по ред и по тип. Това е удобно, обаче си има ограничение, трябва всички членове към които се обръщаме да са видими за нас във външния свят. Тук се намесва проблемът с класовете... 

При класовете този подход с инициализиращия списък няма да ни свърши работа поради две причини: 

- Данните на един клас могат да бъдат поставени в **private** полето му и съответно да са невидими за нас при създаване на обект...
- Може и най-вероятно искаме да се случи някаква специална обработка на тези данни при създаване на обект на класа...

Поради това тук се намесва конструктора. Това беше специална функция без тип на връщане и носеща името на класа, в който се помещава. Конструкторите и функционалностите към тях, които C++ ни предоставя са много и точно те ще бъдат обект на нашето упражнение. 

Ето същия пример, посочен отгоре, но имплементиран чрез клас и няколко overload-a на конструктор, приемащи различен по брой параметри:

~~~.cpp
class Circle{
public:

    // Конструктор по подразбиране, без инициализация на член данни
    Circle() {}

    // Конструктор само с 2 аргумента, извършва се специална логика
    Circle(int radius, int point)
    {
        this->radius = radius;
        this->center_x = this->center_y = point;
    }

    // конструктор, приемащ всички аргументи, които му трябват 
    Circle(int radius, int c_x, int c_y)
    {
        this->radius = radius;
        this->center_x = c_x;
        this->center_y = c_y;
    }

    int diameter() const { return radius * 2; }

// Стойностите ни вече са скрити от външния свят
private:
    int radius;
    int center_x;
    int center_y;
};
~~~

Сега когато се опитаме да създадем обект от тип Circle, компилаторът ще се обърне към дефинираните конструктори и ще се опита да намери подходящ за нашето извикване:

~~~.cpp
int main(void){
    Circle c1();             // това не е конструктор по подразбиране, възприема се като извикване на функция!!
  
    Circle c2;               // това вече ще извика конструкторът по подразбиране ако има такъв...
    Circle c3(1,2,3);        // това ще извика конструкторът, приемащ три параметъра
    Circle c4(1,2,3,4,5,6);  // това ще доведе до грешка, няма дефиниран такъв конструктор 
    return 0;
}
~~~

<a id="1.2"></a>
**Използване на инициализиращ списък**

При извикването на конструктор от какъвто и да е било тип имаме правото да използваме незадължителен инициализиращ списък. Чрез него можем да инициализираме с конкретни стойности член данни на обекта още преди да се е изпълнило тялото на конструктора или да делегираме някой друг конструктор, дали с цел наследяване, дали с цел валидиране на обекта.   
  
Силно се препоръчва този подход на инициализация на данните, тъй като ако искаме да задаем стойности на константни член данни или такива, които са референция към нещо външно, това може да се случи единствено и само в инициализиращия списък...

Ето пример как бихме реализирали някои от горните конструктори с инициализиращ списък:

~~~.cpp
Circle(int radius, int c_x, int c_y)
    : radius(radius)
    , center_x(c_x)
    , center_y(c_y) {  }
~~~

<a id="1.3"></a>
### Видове конструктори

Нека пристъм към разглеждането на различните типове конструктори, които ни предоставя C++

<a id="1.3.1"></a>
**Конструктор по подразбиране (Default Constructor)**

Това са конструктори, които по дефиниция нямат никакви аргументи, но могат да имат параметри със стойност по подразбиране.

~~~.cpp
class Circle {
public:

    // Конструктор по подразбиране 
    Circle() {
        // логика за инстанциране 
    }

    // Това също е конструктор по подразбиране, но с 3 параметъра със стойности по подразбиране 
    Circle(int agr1 = 0, int arg2 = 1, int arg2 = 2) {
        // логика за инстанциране
    }    
...
};
~~~

Много важна подробност е, че ако един клас няма дефиниран конструктор по подразбиране, то тогава ще му се създаде служебен такъв, който ще използва стойностите по подразбиране, ако има такива, на член данните ни или конструкторите по подразбиране, ако са обекти. 

~~~.cpp
#include <iostream>

class Circle{
public:
    int diameter() const { return radius * 2; }

private:
    int radius  { 1 };
    int center_x{ 0 };
    int center_y{ 0 };
};

int main(void){
    Circle c;
    /*
    *  Създава се обект от тип Circle, за който се извиква служебен inline конструктор по подразбиране.
    *  Стойностите му ще бъдат каквито са зададени в къдравите скоби, а на конзолата ще се принтира 2
    */
    std::cout << c.diameter();
    return 0;
}
~~~

Има случаи, обаче, когато няма да се създаде служебен конструктор по подразбиране.   

Един от тях е ако ние експлицитно окажем, че не искаме да се генерира такъв, това може да стане по два начина или използваме ключовата дума **delete**, или го поставяме в скритата (**private**) секция на нашия клас, като вторият подход се е използвал, преди да се добави запазената дума **delete** и затова първият е за предпочитане.

~~~.cpp
class Circle{
    Circle();           // --> скриване на конструктора от външния свят
public:
...
};

или

class Circle{
public:
    Circle() = delete; // --> експлицитно оказваме, че не искаме такъв конструктор
...
};
~~~

Друг случай, на който може да попаднем е, когато някои от нашите член данни не могат да се конструират по подразбиране. Това се получава ако някое поле е от константен тип или референция и няма зададена стойност в инициализиращия списък на някои от нашите конструктори или ако поле е от тип друг клас и няма конструктор по подразбиране, или деструктор. Тогава нашият служебен конструктор ще бъде дефиниран с `= delete`...

~~~.cpp
// Този клас не може да се инициализира по подразбиране има константно поле id 
class Circle{
public:
...
private:
    const int id;
};

// Това е клас, в който експлицитно е оказано, че не може да се инициализира по подразбиране 
class Point{
public:
    Point() = delete;
private:
    int x, y;
};

// За този клас няма да се генерира конструктор по подразбиране, тъй като Point няма конструктор по подразбиране
class Square{
public:
...
private:
    int area;
    Point center;
};

int main(void){

    Square s; // не е възможно
    Point  p; // и това не е възможно
    Cirlce c; // както и това...

    return 0;
}
~~~

Посленият случай, в който може на наш клас да не се генерира служебен конструктор по подразбиране е, когато имаме дефиниран както и да е било конструктор, който не по подразбиране.

Ето пример:

~~~.cpp
class Circle{
public:

    /*
    *  Имаме дефиниран конструктор с 3 аргумента
    *  => няма да получим конструктор по подразбиране...
    */
    Circle(int radius, int c_x, int c_y)
        : radius(radius)
        , c_x(c_x)
        , c_y(c_y) {}

private:
    int radius;
    int c_x, c_y;
};

int main(void) {
    Circle c; // няма да проработи
    return 0;
};
~~~

<a id="1.3.2"></a>
**Копиращ Конструктор (Copy Constructor)**

Копиращите конструктори са неразделна част от жизненият цикъл на един обект, но най-вероятно досега не сте се сблъсквали с тази концепция ако сте писали на други езици, тъй като по-голямата част от логиката остава скрита от пишещия, но в C++ имаме честта сами да ръководим как нашите данни биват копирани между обекти, за да си подсигурим правилен инвариант и собстевност на данните между обекти от един и същи тип. 

Копиращия конструктор на един обект бива извикан при инициализация на обект чрез друг или при експлицитно създаване на обект с кръгли скобки и подаден аргумент друг обект. Ето няколко пример:

~~~.cpp
class Circle{
public:
    Circle()  : radius(1), c_x(0), c_y(0) {};
    Circle(int radius, int c_x, int c_y) : radius(radius), c_x(c_x), c_y(c_y) {};

private:
    int radius;
    int c_x, c_y;  
};

int main(void) {
    Circle c1;
    Circle c2(10,1,1);

    // Това представлява извикване на копиращия конструктор на Circle за инстанциране на обекта c3, чрез данните на обекта c1
    Circle c3 = c1;

    // Това е абсолютно същото извикване като горното, но тук извикваме конструктора експлицитно с обекта c2
    Circle c4(c2);

    return 0;
};
~~~

И в двете извиквания, фигуриращи в примера се извиква служебния копиращ конструктор зададен на класа Circle, който в случая прави така наречено плитко копиране (**shallow copy**) на данните на обекта, който се копира. Така че логиката ни е стабила, поради факта че нашите обекти от тип Circle се състоят само от скаларни по тип данни. 

Обаче в момента, в който добавим каквито и да е динамични член данни към класа Circle, тази логика няма да бъде коректна вече, тъй като плиткото копиране ще ни доведе до споделена собственост на динамичните данни между двата обекта - копирания и копиращия. Това може да доведе до много сериозни проблеми ако не е умишлено, като например двойно изтриване, хвърчащи референции и указатели и в най-лошия случай **use after free** грешки. 

Ето конкретен пример с това, кога служебният копиращ конструктор няма да ни свърши работа:

~~~.cpp
#include <string.h>
#include <iostream>

class Book {
public:
    Book() : text(nullptr) {};
    Book(const char* text)
    {
        if(!text) std::invalid_argument("Invalid data!");

        this->text = new char[strlen(text) + 1];
        strcpy(this->text, text);
    };    

    ~Book() 
    { 
        delete[] text; 
        text = nullptr;
    }

private:
    char* text; 
};

int main(void){

    try
    {
        Book bible("And there was light!");

        {
            // Извикване експлицитно на служебен копиращ конструктор 
            Book necronomicon(bible); 
        } // --> край на блока, извиква се деструктор на necronomicon

        // Какво става тук??
        // necronomicon изтри данните на bible...
        // Това изобщо не е желан ефект
    }
    catch(const std::exception& e)
    {
        std::cout << e.what();
    }
    return 0;
};
~~~

При такива ситуации имаме нужда от това да си дефинираме наш собстевн копиращ конструктор. Но преди да ви покажа как става това нека рагледаме какъв вид точно има копиращия конструктор и как можем да го дефинираме...

Имаме следния синтаксис, когато дефинираме копиращи конструктори:

~~~.cpp
Circle(Circle& other); // Това е позволено, но не го използвайте, защото позволява промяна на обекта, от който копираме, а това за нашите цели няма да се очаква!!!
Circle(const Circle& other); // Това е конструктора, който главно ще използваме 

// От тези двата ще се абстархираме, за сега просто информативно да знаете, че съществуват 
Circle(volatile Circle& other);
Circle(volatile const Circle& other);

// Позволяват се допълнителни параметри, но само със стойности по подразбиране, няма да го срещнете често...
Circle(Circle& other, int i = 42, string label = "Box");
~~~

Това представлява прототипа на всеки копиращ конструктор, като може да имаме няколко копиращи конструктора, стига да са с различна дефиниця от показаните. Всеки клас има служебен копиращ конструктор от втория тип, който прави плитко копие. 

Нека сега разпишем примера отново, но този път правилно...

~~~.cpp
#include <string.h>
#include<iostream>

class Book {
public:
    Book() : text(nullptr) {};
    Book(const char* text)
    {
        if(!text) throw std::invalid_argument("Invalid data!");

        this->text = new char[strlen(text) + 1];
        strcpy(this->text, text);
    };    

    // Дефинираме копиращ конструктор
    Book(const Book& other)
    {
        text = new char[strlen(other.text) + 1];
        strcpy(text, text);
    }

    // Забелязахте ли колко много логиката си прилича с конструктора? Нека преизползваме малко логика (;
    // Book(const Book& other)
    //     : Book(other.text) {};

    ~Book() 
    { 
        delete[] text; 
        text = nullptr;
    }

private:
    char* text; 
};

int main(void){

    try
    {
        Book bible("And there was light!");

        {
            // Извикване експлицитно на служебен копиращ конструктор 
            Book necronomicon(bible); 
        } // --> край на блока, извиква се деструктор на necronomicon

        // Какво става тук??
        // necronomicon изтри своето собстевно копие на данните на bible
        // Това вече е желан ефект => Успех!!
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    
    return 0;
};
~~~

Това е много удобна функционалност, която ни позволява да дефинираме каквито си искаме типове и да контролираме техните взаимоотношения по времето на нашата програма.
 
Има няколко важни подробности, които трябва да знаете. Както при конструкторите по подразбиране така и тук може да не получим служебен копиращ конструктор, като причините са доста сходни:

- Експлицитно сме го изтрили с `= delete`...
- Имаме член данни, които не могат да се копират (нямат дефиниран какъвто и да е копиращ конструктор...)

Ето някои примери:

~~~.cpp
#include <iostream>

class Point {
public:
    Point(int x, int y) : x(x), y(y) {};
    Point(const Point& other) = delete;   // експлицитно изтрит копиращ конструктор
private:
    int x, y;
};

class Circle{
public:
    Circle(std::istream& is = std::cin) : is(is), radius(10), cords({0,0}) {};
private:
    std::istream& is; // имаме референция към файл, това е позволено, тъй като референцията просто ще се прехвърли

    int radius;
    Point cords;      // това обаче ще изтрие служебния копиращ конструктор на Circle...
};

int main(void){
    Circle c1;
    Point p1(1,1);

    Circle c2 = c1;
    Point p2 = p1;

    return 0;
};
~~~

<a id="1.3.3"></a>
**Move Constructor**

Това е специален тип конструкор, който се добавя към езика във версия C++ 14 и по принцип не се очаква от вас да го използвате на каквито и да е изпити ако се интересувата от тези неща разбира се. Иначе като оставим настрана академичните страдания, почти на 100% задължително е да знаете както е това, защото на практика се използва постоянно. Move семантиката на C++ ни позволява да преместваме собстеност на данни между обекти от един и същи тип без да се копира. Най-интуитивно можете да си представите, че единият обект краде данните на другия умиращ от старост обект, защото вече не са му нужни. Този конструктор се използва върху RValue reference стойности или на български, временни обекти. Това са обекти, които не са закачени към променлива и им предстои извикване на деструктор. 

Ето това е пример за RValue reference:

~~~.cpp
struct Point { int x, y; };

void foo(Point&& rvalue_ref) {}

int main(void)
{
    // Създава се временен обект от тип Point, който не е асоцииран към никаква променлива.
    // На него му предстои да бъде унищожен след извикването на функцията!  
    foo({1,2});
}
~~~

Както виждате RValue reference означваме с &&, такава ще бъде и сигнатурата на Move конструктора. Целта на този конструктор ще бъда да вземем всички данни от някой обект, на който му предстои да бъде изтрит и да ги запазим в нов обект без да извършваме каквото и да е било копиране. Важна подробност е, че можем да предизвикаме изтриването на обект по всяко време, като използваме една функция от стандартната библиоткеа на име std::move(<object>). Нейната роля е да направи подадения обект RValue reference при подаване във функции.

Ето пример за използване на Move конструктор:

~~~.cpp
#include <iostream>
#include <new>
#include <string.h>
#include <stdexcept>

class Book{
public:
    Book() : author(nullptr), ISBN(nullptr), title(nullptr) {};
    Book(const char* author, const char* ISBN, const char* title)
        : Book() // --> делегиране на конструктор с цел валидни стойнсоти на полетата 
    {
        if(!set_string(this->author, author) ||
           !set_string(this->ISBN, ISBN)     ||
           !set_string(this->title, title))
        {
            free();
            throw std::invalid_argument("Invalid arguments!");
        }
    }

    Book(const Book& other) = delete; // не искаме да се копира, за илюстрационни цели
    Book(Book&& other)
        : Book() // --> делегиране на конструктор с цел валидни стойнсоти на полетата 
    {
        // Тук мога да използвам std::swap, защото делегираният конструктор е направил всяко поле nullptr.
        // След като излем от конструктора ще се извика деструктора на other и данните му ще се зачистят.
        // Ако имах неинициализирани адреси, деструктора на other щеше да хвърли segmentation fault!!
        std::swap(author, other.author);
        std::swap(ISBN, other.ISBN);
        std::swap(title, other.title);

        // може и така по ваша преценка 
        // author = other.author;
        // ISBN = other.ISBN;
        // title = other.title;

        // other.author = other.ISBN = other.title = nullptr;
    }

    ~Book() { free(); }


private:
    static bool set_string(char*& where, const char* what)
    {
        if(!what) return false;

        char* temp = new(std::nothrow) char[strlen(what) + 1];
        if(!temp) return false;

        strcpy(temp, what);
        delete[] where;
        where = temp;
        return true;
    } 

    void free() { delete[] author; delete[] ISBN; delete[] title; }

    char* author;
    char* ISBN;
    char* title;
};


int main(void)
{
    try
    {
        Book the_book_of_truth("Unknown", "???", "The Book Of Truth");
        Book thief(std::move(the_book_of_truth));
    }
    catch(const std::exception& e)
    {
        std::cout << e.what();
    }
    
    
    // Извикваме Move конструкотра с std::move
    
    // След този ред данните на the_book_of_truth са прехвърлени в thief, а всяко поле на the_book_of_truth е затрито, 
    // обекта е невалиден вече 
}
~~~

Служебен Move конструктор се създава за класове, за които не сме дефинирали експлицитно:

- Копиращ конструктор или присвояващ оператор (до него ще стигнем след малко)
- Move присвояващ оператор (и него ще обясня)
- Деструктор

Също така ако няма какъвто и да е Move конструкотр, операции които изискват такъв ще използват копиращия конструкотр, ако има такъв...

<a id="1.3.4"></a>
**Експлицитни конструктори (Excplicit Constructors)**

Експлицитните конструктури представляват нещо като casting конструктори. Идеята е, че ако имаме конструктор, който приема само един параметър или само един параметър в списъка на конструкотра не е със стойност по подразбиране, тогава може да създадем обект от типа на параметъра, като той ще се cast-не до класа ни.

Най-добре се разбира с пример:

~~~.cpp
#include <string.h>
#include <new>

class Person{
public:

    explicit Person(bool sex, int age = 20, const char* str = "John Doe")
        : sex(sex)
        , age(age)
        , name(copy_str(str)) {};

    Person(const Person&)               = delete;
    Person& operator = (const Person&)  = delete;

    ~Person() { delete[] name; } 

private:
    static char* copy_str(const char* what)
    {
        if(!what) return nullptr;

        char* temp = new(std::nothrow) char[strlen(what) + 1];
        if(!temp) return nullptr;

        strcpy(temp, what);
        return temp;
    }

    const char* name;
    int age;
    bool sex; // 1 за мъж, 0 за жена
};


int main(void)
{
    // Това създава обект от тип Person с име John doe, години 20 и пол мъж
    //Person pesho = 1;

    // Това създава обект от тип Person с име John doe, години 20 и пол жена
    //Person peshka = 0;

    // Ако пред конструктора я нямаше думата explicit кода тук щеше да е позволен,
    // за да не стават грешки оабче, се е добавила думата explicit, за да не може това 
    // създаване на обекти да е валидно!
}
~~~

<a id="1.4"></a>
### Оператори за присвояване 

Както вече видяхте, съществуват оператори за присвояване в C++, като тяхната идея е както копиращия конструктор да копират стойностите от един обект на друг, но тук разликата е, че и двата обекта, с които работим са инициализирани, което значи че са минали успешно през конструкотр и имат валидни стойности. Това са живи обекти, за разлика от копиращия конструкотр и Move конструкотра, където единия обект тепърва създаваме.

По принцип операторите за присвояване са точно предефиниране на оператор = в езика, което означава, че ще пренаписваме логиката на това какво става, когато напишем един обект да е равен на друг. По-задълбочено с предефиниране на оператори ще се занимаваме след време, но за целите на това да създадем работещ жизнен цикъл на един обект, трябва да можем да окажем какво става, когато присвоим един обект на друг...

<a id="1.4.1"></a>
**Copy Assignment Operator**
Това е бинарна операция, което означава, че работи с два елемента, единия от които е обекта, в който се помещаваме (лявата страна на равенството), а другия е обекта, от който ще копираме (дясната страна на равенството). Самият оператор има следната сигнатура:

~~~.cpp
Type& operator = (const Type& other);
~~~

Типът на връщане е референция към нашия обект, тъй като искаме да рефлектираме поведението на истинския оператор за присвояване, който връща референция, към стойността в ляво на равенството, за да можем да правим верижни присвоявания като това:

~~~.cpp
obj1 = obj2 = obj3
~~~

Много е важно да не бъркаме операцията присвояване с извикване на копиращ конструктор, тъй като двете операции са коренно различни. Копиращия конструктор работи върху неинициализирани обект, а оператора за присвояване върху вече инициализирани обекти.

Ето пример, за тяхната разлика:

~~~.cpp
class Circle{
public:
    Circle& operator = (const Circle& other)
    {
        if(this != &other){
            radius = other.radius;
            c_x    = other.c_x;
            c_y    = other.c_y;
        }

        return *this;
    };

private:
    int radius { 1 };
    int c_x    { 0 };
    int c_y    { 0 };
};

int main(){
    // Извикване на конструктор по подразбиране 
    Circle c1;

    // Извикване на копиращ конструктор 
    Circle c2 = c1;

    // Извикване на конструктор по подразбиране
    Circle c3;

    // Извикване на оператор за присвояване!!
    c3 = c2;

    return 0;
};
~~~

<a id="1.4.2"></a>
**Move Assignment Operator**

Това отново е бинарен оператор за присвояване, но тук идеята отново е че обектът в дясно на равенството е RValue reference и трябва да откраднем стойностите му без да копираме каквото и да е. Единствената разлика с Move конструктора е, че този път обектът в ляво е инициализиран и има реални стойностите за член данните, така че ако разменим стойностите на крадеца и "смъртника", когато се извика деструктора на обекта в дясно, той ще зачисти данните не на себе са, а на този, който е бил в ляво.

Ето пример:

~~~.cpp
// какво става ако има динамични данни
class Stack{
public:
    Stack(int cap = 10)
        : cap(cap)
        , tos(0)
    {
        data = new int[cap]{};
    }

    Stack(const Stack& other) = delete;  // за илюстрационна цел
    Stack(Stack&& other)      = delete;

    Stack& operator = (const Stack& other)
    {
        if(this == &other) return *this;

        int* temp = new int[other.cap]{};
        for (int i = 0; i < other.cap; i++)
        {
            temp[i] = other.data[i];
        }
         
        delete[] data;
        data = temp;
        cap  = other.cap;
        tos = other.tos;

        return *this;
    }

    Stack& operator = (Stack&& other)
    {
        if(this == &other) return *this;

        std::swap(data, other.data);
        std::swap(tos, other.tos);
        std::swap(cap, other.cap);

        return *this;
    }

    ~Stack() { delete[] data;}

    void add(int elem)
    {
        if(tos == cap) throw std::invalid_argument("Full!");
        data[tos++] = elem;
    }

    int remove()
    {
        if(tos == 0) throw std::invalid_argument("Empty!");
        int val = data[tos--];
        return val;
    }

private:
    int* data;
    int tos, cap;
};

int main(void){
    Stack s2;
    s2.add(2);

    Stack s3;
    s3.add(10);
    s3.add(12);
    s3.add(1000);

    // 1) Стойностите на s2 се прехвърлят на s3
    // 2) Стойностите на s3 се прехвърлят на s2
    // 3) Извиква се деструктор на s2 и данните на s3 изчезват! 
    s3 = std::move(s2);

    // s3 има единствен елемент 2, а s2 е унищожен
    return 0;
}
~~~

<a id="1.5"></a>
### The Rule of X

В C++ през години са появили няколко практики, които ни оказват какво трябва един клас да имплементира и какво не. Най-главното, което ще се изисква от вас е **The Rule of 3**, което гласи, че ако напишете логиката, за кое да е едно от тези три: **Copy Constructor**, **Assignment operator** или **Destructor**, трябва да имате имплементирана или поне изяснена и логиката за останалите две. Може да го запомните по този начин - ако имате динамични данни ще се наложи да имплементирате деструктор, което влече след себе си копиращия конструктор и оператора за присвояване. 

Понякога има случай, когато един обект не може да се копира или не може да си сменя стойността след създаване, но пак да има динамични данни. Тогава имплементирате каквото можете, а другото съответно забранявате с `delete`. Можем да обобщим и останалите правила, за които не споменах, тъй като няма да се изискват от вас, в ето тази табличка:

### Правила за жизнения цикъл на обекти в C++

| Функционалност | Rule of Three | Rule of Five | Rule of Four and a Half |
| :--- | :---: | :---: | :---: |
| **Копиращ конструктор** | x | x | x |
| **Оператор за присвояване** | x | x | x |
| **Деструктор** | x | x | x |
| **Move конструктор** | - | x | x |
| **Move оператор за присвояване** | - | x | - |
| **Swap функция** | - | - | x |

---

> **Бележка:** При **Rule of Four and a Half** (Copy-and-Swap), в оператора за присвояване се създава копие на подадения за копиране обект и чрез move конструктора се разменят стойностите на текущия обект и копирания

---

<a id="2"></a>
## Изключения

C++ както много други езици като JAVA и C# ни предоставя възможността да използваме изключения (exceptions), с които да обработваме грешки по време на изпълнението на нашия код. В сърцето си изключенията представляват механизъм, чрез който можем да прескочим обратно в call stack-а на нашата програма до място, където да обработим съответната грешка. Това място, за което говоря е така наречената **try/catch** конструкция.

Както може би сте забелязали ако прочетохте записките до тук, аз вече използвам изключения в примерите. За да работим с тях в C++, трябва да включим header файла **stdexcept** в него са дефинирани различните класове, репрезентиращи изключения в езика. За да раборим с тези изключения трябва да използваме запазената дума **throw** последвана от потенциално изключение или само текст. Throw ни връща обартно по стека, до първият try/catch блок, който намери, където от нас се очаква по някакъв начин да обработим грешката. която ни е подадена. Ако не намери такъв блок, програмата просто се терминира...

Едно от най-важните неща, които трябва да запомните е, че след като бъде хвърлено изключение, всички **вече създадени обекти в scope** ще бъдат унищожени, така че когато използваме изключения в жизнения цикъл на някой обект, задължително трябва да се подсигурим, че сме си изчистили ресурсите преди да хвърлим изключението, тъй като в момента на хвърляне обектът няма да е създаден и съответно зачистен...

Ето пример за използване на изключения:

~~~.cpp
#include <stdexcept>
#include<iostream>
#include <string.h>

class Book {
public:
    Book(const char* title, const char* author)
        : title(nullptr)
        , author(nullptr)
    {
        // Тук нямаме проблеми, само new може да хвърли изключение и ще го обработим, 
        // но памет заделена няма да има...
        this->title = strcpy(new char[strlen(title) + 1], title);

        this->author = new(std::nothrow) char[strlen(author) + 1];
        if(!this->author)
        {
            // зачистваме си ресурсите и излизаме
            // Ако new се провали, то хвърля bad_alloc, следователно и ние това ще хвърлим...
            delete[] this->title;
            throw std::bad_alloc();
        }
        
        strcpy(this->author, author);
    }
    
    Book(const Book& other)
        : Book(other.title, other.author) {}

    Book& operator = (const Book& other)
    {
        if(this == &other) return *this;

        char* temp1 = strcpy(new char[strlen(other.title) + 1], other.title);
        char* temp2 = new(std::nothrow) char[strlen(other.author) + 1];
        if(!temp2)
        {
            delete[] temp1;
            throw std::bad_alloc();
        }

        delete[] title;
        delete[] author;

        title = temp1;
        author = temp2;
        return *this;
    }

    ~Book() { delete[] title; delete[] author; }

private:
    char* title;
    char* author;
};

int main(void) {

    try{
        Book b1("Moby-Dick", "Herman Melville");
    }
    catch(const std::exception& e) {
        std::cerr << e.what() << '\n';
        return -1;
    }

    return 0;
}
~~~

От тук нататък така ще работим с класове. Един конструктор трябва да създаде валиден обект или да не създаде нищо, този механизъм може да реализираме идеално чрез изключения както се вижда нагледно в примерите. Единствено и само трябва да се внимава с зачистването на ресурсите...

<a id="2.1"></a>
**Гаранции за сигурност**

На различни места нивата на сигурност, коието можем да имплементираме чрез изключения се делят на три:

- Nothrow/Nofail
- Strong exception safety
- Weap/Basic exception safety
- No guarantee (няма да се коментира, предполагам е ясно)

Ще разгледаме всяко ниво и ще добавим съответно и пример към него:

<a id="2.1.1"></a>
**Nothrow**

Това е концептуално най-сигурното ниво (според microsoft), което може да постигнем с изключения. Обаче няма как да сме сигурни, че това е в сила освен ако всяка функция, извикана след сегашната също не гарантира, че няма да хвърли изключения или улавяме всички изключения преди да сме достигнали до тази функция...

По стандарт деструкторите, на кой да е клас винаги трябва да бъдат noexcept, като в C++ има добавена запазена дума, която поставяме след функции, за да гарантирам именно това, че функцията няма да хвърли изключения, каквото и да се случи. Благодарение на тази ключова дума, компилаторът може да направи много оптимизации.

~~~.cpp
~Type() noexcept
{
    // cleanup
}
~~~

Други фунцкии от жизненият цикъл, които се очаква да бъдат noexcept са Move конструкторът и Move операторът за присвояване.

<a id="2.1.2"></a>
**Strong exception safety**

Силната гаранция за сигурност гласи, че ако бъде хвърлено изключение по време на изпълнение на програмата, няма да има никакви течове на памет и състоянието на обекта, върху който работим ще остане непроменено. Като пример можете да вземето присвояването ако се хвърли изключение по време на присвояване, то няма да настъпят никакви промени и по двата обекта само и единствено данните ще бъдат зачистени...

Ето пример, при потенциална грешка данните остават непроменени:
~~~.cpp
Book& operator = (const Book& other) {
    if(this == &other) return *this;

    char* temp1 = strcpy(new char[strlen(other.title) + 1], other.title);
    char* temp2 = new(std::nothrow) char[strlen(other.author) + 1];
    if(!temp2)
    {
        delete[] temp1;
        throw std::bad_alloc();
    }

    delete[] title;
    delete[] author;

    title = temp1;
    author = temp2;
    return *this;
}
~~~

<a id="2.1.3"></a>
**Weak Exception Safety**

Тази гаранция гласи, че ако се хвърли изключение, то няма теч на памет и обектът все още е във валидно състояние, обаче данните му може да са променени. Този подход е много по-често от силния, тъй като обикновенно излиза прекалено скъпо в памет и време да се гарантира пълна сигурност...

Ето пример със същи код, данните ще се променят:
~~~.cpp
Book& operator = (const Book& other) {
    if(this == &other) return *this;

    delete[] title;
    delete[] author;
    title = author = nullptr;

    // Изтрили сме Старите данни, от тук нататък ако имаме изключение обектът все още ще е валиден, просто с променени данни

    title = strcpy(new char[strlen(other.title) + 1], other.title);
    author = strcpy(new char[strlen(other.author) + 1], other.author);

    title = temp1;
    author = temp2;
    return *this;
}
~~~

Имената на тези гаранции са взети от microsoft стандартите, на много места носят различни имена в курса се използват следните имена:

- Strong exception safety - силна/строга гаранция за сигурност
- Weak exception safety - слаба гаранция за сигурност
- No guarantee - без гаранция

<a id="3"></a>
## Ресурси 

- https://learn.microsoft.com/en-us/cpp/cpp/constructors-cpp?view=msvc-170
- https://learn.microsoft.com/en-us/cpp/cpp/assignment?view=msvc-170
- https://stackoverflow.com/questions/45754226/what-is-the-rule-of-four-and-a-half
- https://en.cppreference.com/w/cpp/language/member_functions.html#Special_member_functions
