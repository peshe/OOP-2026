# Задачи

### Теоретична задачка
За всеки ред, кажете дали ще се изпълни или ще даде грешка. Ако се изпълни, какво ще изведе на конзолата?  
```cpp
template <typename T>
void func(T a, T b)
{
	std::cout << a << b;
}

int main()
{
	func<int>(1, 2);        //1
	func(1, 2);             //2
	func<double>(1.5, 2);   //3
	func(1.2, 2);           //4
	func(3, 'a');           //5
	func<int>(3, 'a');      //6
	func<int>(3, 010);      //7
}
```
### Задача 1
Напишете шаблон на функция, която да връща сумата на две променливи от един и същи произволен тип. (`T sum(T a,T b)`)

### Задача 2
Напишете шаблон на функция, която да разменя стойностите на две променливи от един и същ произволен тип.

### Задача 3
Напишете шаблон на функция, която да принтира масив с променливи от произволен тип. Да се направи специализация за низове (char*).

### Задача 4
Напишете шаблон на функция, която да приема като параметри "наредена тройка" от произволни елементи, т.е. 3 променливи които може да **не са** от един и същи тип. А може и да са :D. Да се принтират на конзолата.

### Задача 5
Напишете шаблон на функция, която да казва дали даден елемент се съдържа в даден масив от същия тип. Елементът, масивът и размерът му се подават като параметри.

### Задача 6
Напишете функциите `map` и `filter` (а може и `fold`), които са ви познати от седмица 1, само че да работят с масив от елементи с произволен тип.
