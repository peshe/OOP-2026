# Задачи

### Задача 0
Върнете се на задачите от миналата седмица и напишете конструктор с параметри на класа `Student` от задача 1.

### Задача 1
Направете класа `Student` от задача 1 с динамично заделен низ за член-данната `name`.  
Напишете му:
-default-ен конструктор  
-конструктор с параметри  
-копиращ конструктор  
-оператор за присвояване (оператор=)  
-деструктор  
-нов setter за `name`, който коректно да работи с динамичната памет!  

Грижете се за заделяне и освобождаване на динамична памет.

### Задача 2
Направете клас `Beverage` (напитка), която има следната информация:  
-име (динамично заделен низ)  
-цена  
-съдържа ли алкохол или не  

Да се напишат:
конструктори (по подразбиране, с параметри, копиращ), деструктор, оператор=, getter-и и setter-и, функция за принтиране на екрана.  

Грижете се за заделяне и освобождаване на динамична памет.

### Задача 3
Да се направи клас `Phone`.  
Като характеристики (член-данни), той ще има:  
1.модел - низ до 32 символа  
2.ниво на батерия - цяло число (0-100)  
3.OS version - цяло положително число  
4.ниво на звука - цяло число (0-10)  

Модела не може да се променя след създаване на обекта, ОС версията само чрез член-функцията `update`, нивото на батерията също не може да се променя пряко от външния свят.

При извикването на някои функции се хаби батерия на телефона. Ако батерията падне до 0, телефонът се изключва и повече не може да го ползваме, докато не го заредим. При писането на всяка функция подсигурете, че телефонът има батерия. Ако няма, функцията не прави нищо.  

При създаване на обект, се инициализират модела, нивото на батерия и OS версията.

Реализирайте следните член-функции:  
-getter-и (мислете за `const`)  
-подходящи setter-и (помислете добре кои член-данни трябва и не трябва да имат)  
-default constructor  
-конструктор с параметри  
-printPhone  
-watchReels(int min) (за всяка минута гледане се хаби 1% батерия)  
-playClash(int min)  (за всяка минута игра се хаби 3% батерия)  
-takePhoto  (хаби 1% батерия)  
-update   (увеличава версията на OS с 1, хаби 10% батерия, ако телефона няма необходимото ниво на батерия, функцията не прави нищо)  
-charge(int min) (за всяка минута зареждане, нивото на батерията се покачва с 1%)  

**БОНУС: направете модела на телефона с динамично заделен низ**, добавете/променете нужните член-функции за да подсигурите коректно управление на динамичната памет.  
направете си базовата задача преди да почнете бонуса обаче (освен ако не се чувствате супер смели лол)

### Задача 4
Разбрали сте наскоро, колко е богат Jeff Bezos и решавате, че искате да направите следващия Amazon.com  
За целта реализирайте клас `Product`, който ще продавате в сайта. Той трябва да съдържа информация за:  
-име - динамично заделен низ  
-описание - динамично заделен низ  
-цена  
-дали е наличен в склада  

Направете подходящи методи за достъп и промяна на член-данните (getters, setters), както и всички функции, които са нужни за осигуряване на валиден жизнен цикъл (c-tor, d-tor...)  
Направете и функция за принтиране на продукт  
Направете функция `canBuy(float availableMoney)`, която да казва дали продуктът може да бъде купен. Това е възможно ако наличните пари (от параметъра) са достатъчни и продуктът е наличен в склада.